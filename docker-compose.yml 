# docker-compose.yml
version: '3.8'

services:
  service-navigational-list:
    build: .
    container_name: service-navigational-list
    restart: unless-stopped
    ports:
      - "3006:3006"
    env_file:
      - .env
    networks:
      - ngx-net

networks:
  ngx-net:
    external: true